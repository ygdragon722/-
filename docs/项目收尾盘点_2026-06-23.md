# 项目收尾盘点 · 2026-06-23

## 结论

《红楼幻梦》已经从“持续修体验”进入“发布与线上验证”阶段。主流程可以从现代开场完整走到终局，关键人物图、场景图、字幕节奏、回退链路和终局流程都已经经过实机验证。

不建议继续加人物、加支线或扩系统。当前最有价值的下一步是发布前整理、最终抽检和入口验证。

## 已完成并验证

- 现代女性玩家游览仿建大观园、意外入梦的开场已经补上。
- 第一天凤姐、黛玉、王夫人三场人物辨识度和镜头关系已复核。
- 第二天贾母、王夫人佛堂的近景贴图边缘问题已处理。
- 第三天两个抉择页风格统一，文字可读。
- 终局页可逐段推进，回退链路可退回“抉择二 · 女孩”。
- “从头再读一遍”可回到现代开场。
- 全流程冒烟从空存档走到终局末尾，最终存档停在 `stage: ending`。
- `npm run build` 已通过。
- 生产预览 `http://127.0.0.1:4176/` 已抽检，`manifest.webmanifest` 和 `sw.js` 均为 200，未发现资源 404 或控制台错误。
- 已沉淀 `visual-novel-scene-audit` skill，后续画面验收可以复用同一套方法。

## 还需要收尾

1. **发布目标确认**
   - 当前 README 指向 GitHub Pages：`https://ygdragon722.github.io/-/`
   - 已按现有仓库远端和部署脚本统一发布入口。

2. **线上或生产预览验证**
   - 本地 `npm run build` 和生产预览抽检已通过。
   - 已执行 `npx gh-pages -d dist`，但当前仓库还存在 GitHub Actions Pages 工作流，线上最终生效需以 `main` 推送后的 Pages 部署结果为准。

3. **变更归档**
   - 当前工作区包含代码、文档、美术资产和审计截图等多类变更。
   - 建议提交前按“代码体验修复 / 美术资产 / 验收文档 / skill 沉淀”分组盘点。

4. **轻量文案抽检**
   - 主流程已经通，但发布前仍建议抽查错别字、标点、按钮文案一致性。
   - 重点看开场、Day2Wrap、第三天两个抉择、终局。

## 可交给其他 agent 的任务

- **Claude Code**：按 `/Users/mashengjia/AI-Projects-Portal/agent-skills/visual-novel-scene-audit/SKILL.md` 做一轮独立视觉 QA，不改动，只列风险。
- **Kimi Code**：做文案错别字和标点抽检，输出候选修改，必须由 Codex 或用户再验收。
- **Codex**：负责最终集成、构建、实机验证、发布入口检查。

## 下一步建议

优先做发布前最终抽检：

1. 推送 `main` 触发 GitHub Pages 工作流。
2. 发布后验证线上入口、资源路径、PWA manifest 和手机访问。
